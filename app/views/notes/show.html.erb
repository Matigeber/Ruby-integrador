<p id="notice"><%= notice %></p>

<p>
  <strong>Book:</strong>
  <%= @note.book_id %>
</p>

<p>
  <strong>Title:</strong>
  <%= @note.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @note.transform_to_html.html_safe %>
</p>

<%= link_to 'Edit', edit_book_note_path(@note.book_id,@note.id) %> |
<%= link_to 'Back', book_notes_path(@note.book_id) %>
